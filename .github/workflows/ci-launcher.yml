name: CI Launcher

on:
  workflow_call:
    inputs:
      build-type:
        description: Type of build (Debug or Release)
        type: string
        default: Debug
      ref:
        description: 'Ref (branch, tag, or SHA) to check out'
        type: string
        required: false
      output-root:
        description: Root directory for build outputs
        type: string
        required: false
        default: ""
      enable-windows-arm64:
        description: Enable Windows MSVC ARM64 build
        type: boolean
        required: false
        default: false
      qt-architecture:
        description: Qt architecture
        type: string
        required: false
  workflow_dispatch:
    inputs:
      build-type:
        description: Type of build (Debug or Release)
        type: string
        default: Debug
      force-prepare-yes:
        description: Force prepare job to enable tests/builds
        type: boolean
        default: false
      version:
        description: Launcher version
        required: true
      artifact-name:
        description: Name of the uploaded artifact
        required: true
        default: Linux
      output-root:
        description: Root directory for build outputs
        required: false
        default: ""
      qt-version:
        description: Version of Qt to use
        required: true
        default: 6.10.0
      qt-architecture:
        description: Qt architecture
        required: false
      gpg-private-key:
        description: Private key for AppImage signing
        required: false
      gpg-private-key-id:
        description: ID for the gpg-private-key, to select the signing key
        required: false
      apple-codesign-cert:
        description: Certificate for signing macOS builds
        required: false
      apple-codesign-password:
        description: Password for signing macOS builds
        required: false
      apple-codesign-id:
        description: Certificate ID for signing macOS builds
        required: false
      apple-notarize-apple-id:
        description: Apple ID used for notarizing macOS builds
        required: false
      apple-notarize-team-id:
        description: Team ID used for notarizing macOS builds
        required: false
      apple-notarize-password:
        description: Password used for notarizing macOS builds
        required: false
      sparkle-ed25519-key:
        description: Private key for signing Sparkle updates
        required: false
      msystem:
        description: MSYS2 subsystem to use
        required: false
      windows-codesign-cert:
        description: Certificate for signing Windows builds
        required: false
      windows-codesign-password:
        description: Password for signing Windows builds
        required: false
      vcvars-arch:
        description: Visual Studio architecture to use
        required: true
        default: amd64
      ref:
        description: 'Ref (branch, tag, or SHA) to check out'
        required: false
      path:
        description: 'Path to place the repository'
        required: false
      fetch-depth:
        description: 'Fetch depth (default 0 = full history)'
        required: false
        default: '0'

permissions:
  contents: read

env:
  NODE_NO_WARNINGS: 1
  CHECKOUT_REF: ${{ inputs.ref || github.event.inputs.ref || github.event.pull_request.merge_commit_sha || github.event.pull_request.head.sha || github.sha }}
  OUTPUT_ROOT: ${{ inputs.output-root || github.event.inputs.output-root || format('{0}/build/out', github.workspace) }}

jobs:
  build-launcher:
    environment: ${{ inputs.environment || '' }}
    name: Build (${{ matrix.artifact-name }})
    permissions:
      packages: write

    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-24.04
            artifact-name: Linux
            target-platform: linux
            target-arch: x86_64
            qt-architecture: linux_gcc_64
            qt-modules: qtimageformats qtnetworkauth qtwebchannel qtwebengine qtpositioning qtserialport
            qt-version: 6.10.2
            build-system: make

          - os: ubuntu-24.04-arm
            artifact-name: Linux-aarch64
            target-platform: linux
            target-arch: aarch64
            qt-architecture: linux_gcc_arm64
            qt-modules: qtimageformats qtnetworkauth qtwebchannel qtwebengine qtpositioning qtserialport
            qt-version: 6.10.2
            build-system: make

          # Windows MSVC builds
          - os: windows-2022
            artifact-name: Windows-MSVC
            target-platform: windows
            windows-toolchain: msvc
            vcvars-arch: amd64
            qt-architecture: win64_msvc2022_64
            qt-modules: qtimageformats qtnetworkauth qtwebchannel qtwebengine qtpositioning qtserialport
            qt-version: 6.10.2
            build-system: make

          - os: windows-2025
            artifact-name: Windows-MSVC-2025
            target-platform: windows
            windows-toolchain: msvc
            vcvars-arch: amd64
            qt-architecture: win64_msvc2022_64
            qt-modules: qtimageformats qtnetworkauth qtwebchannel qtwebengine qtpositioning qtserialport
            qt-version: 6.10.2
            build-system: make

          - os: windows-11-arm
            artifact-name: Windows-MSVC-arm64
            target-platform: windows
            windows-toolchain: msvc
            target-arch: aarch64
            vcvars-arch: arm64
            qt-architecture: win64_msvc2022_arm64
            qt-modules: qtimageformats qtnetworkauth qtpositioning qtserialport
            qt-version: 6.10.2
            build-system: make

          # Windows MinGW builds
          - os: windows-2022
            artifact-name: Windows-MinGW-w64
            target-platform: windows
            windows-toolchain: mingw
            msystem: MINGW64
            vcvars-arch: amd64_x86
            build-system: make

          - os: windows-2025
            artifact-name: Windows-MinGW-w64-2025
            target-platform: windows
            windows-toolchain: mingw
            msystem: MINGW64
            vcvars-arch: amd64_x86
            build-system: make

          - os: windows-11-arm
            artifact-name: Windows-MinGW-arm64
            target-platform: windows
            windows-toolchain: mingw
            target-arch: aarch64
            msystem: CLANGARM64
            vcvars-arch: arm64
            build-system: make

          # macOS builds
          - os: macos-15
            artifact-name: macOS-15-universal
            target-platform: macos
            target-arch: universal
            macosx-deployment-target: 12.0
            qt-architecture: clang_64
            qt-modules: qtimageformats qtnetworkauth qtpositioning qtserialport qtwebengine qtwebchannel
            qt-version: 6.10.2
            build-system: make

          - os: macos-26
            artifact-name: macOS-26-universal
            target-platform: macos
            target-arch: universal
            macosx-deployment-target: 12.0
            qt-architecture: clang_64
            qt-modules: qtimageformats qtnetworkauth qtpositioning qtserialport qtwebengine qtwebchannel
            qt-version: 6.10.2
            build-system: make

          - os: macos-15
            artifact-name: macOS-15-arm64
            target-platform: macos
            target-arch: aarch64
            macosx-deployment-target: 12.0
            qt-architecture: clang_64
            qt-modules: qtimageformats qtnetworkauth qtpositioning qtserialport qtwebengine qtwebchannel
            qt-version: 6.10.2
            build-system: make

          - os: macos-26
            artifact-name: macOS-arm64
            target-platform: macos
            target-arch: aarch64
            macosx-deployment-target: 12.0
            qt-architecture: clang_64
            qt-modules: qtimageformats qtnetworkauth qtpositioning qtserialport qtwebengine qtwebchannel
            qt-version: 6.10.2
            build-system: make

          - os: macos-15-intel
            artifact-name: macOS-intel
            target-platform: macos
            target-arch: x86_64
            macosx-deployment-target: 12.0
            qt-architecture: clang_64
            qt-modules: qtimageformats qtnetworkauth qtpositioning qtserialport qtwebengine qtwebchannel
            qt-version: 6.10.2
            build-system: make

          - os: ubuntu-24.04
            artifact-name: Linux-cmake
            target-platform: linux
            target-arch: x86_64
            qt-architecture: linux_gcc_64
            qt-modules: qtimageformats qtnetworkauth qtwebchannel qtwebengine qtpositioning qtserialport
            qt-version: 6.10.2
            cmake-preset: linux
            build-system: cmake

          - os: ubuntu-24.04-arm
            artifact-name: Linux-aarch64-cmake
            target-platform: linux
            target-arch: aarch64
            qt-architecture: linux_gcc_arm64
            qt-modules: qtimageformats qtnetworkauth qtwebchannel qtwebengine qtpositioning qtserialport
            qt-version: 6.10.2
            cmake-preset: linux
            build-system: cmake

          # Windows MSVC builds
          - os: windows-2022
            artifact-name: Windows-MSVC-cmake
            target-platform: windows
            windows-toolchain: msvc
            vcvars-arch: amd64
            qt-architecture: win64_msvc2022_64
            qt-modules: qtimageformats qtnetworkauth qtwebchannel qtwebengine qtpositioning qtserialport
            qt-version: 6.10.2
            cmake-preset: windows_msvc
            build-system: cmake

          - os: windows-2025
            artifact-name: Windows-MSVC-2025-cmake
            target-platform: windows
            windows-toolchain: msvc
            vcvars-arch: amd64
            qt-architecture: win64_msvc2022_64
            qt-modules: qtimageformats qtnetworkauth qtwebchannel qtwebengine qtpositioning qtserialport
            qt-version: 6.10.2
            cmake-preset: windows_msvc
            build-system: cmake

          - os: windows-11-arm
            artifact-name: Windows-MSVC-arm64-cmake
            target-platform: windows
            windows-toolchain: msvc
            target-arch: aarch64
            vcvars-arch: arm64
            qt-architecture: win64_msvc2022_arm64
            qt-modules: qtimageformats qtnetworkauth qtpositioning qtserialport
            qt-version: 6.10.2
            cmake-preset: windows_msvc
            build-system: cmake

          # Windows MinGW builds
          - os: windows-2022
            artifact-name: Windows-MinGW-w64-cmake
            target-platform: windows
            windows-toolchain: mingw
            msystem: MINGW64
            vcvars-arch: amd64_x86
            cmake-preset: windows_mingw
            build-system: cmake

          - os: windows-2025
            artifact-name: Windows-MinGW-w64-2025-cmake
            target-platform: windows
            windows-toolchain: mingw
            msystem: MINGW64
            vcvars-arch: amd64_x86
            cmake-preset: windows_mingw
            build-system: cmake

          - os: windows-11-arm
            artifact-name: Windows-MinGW-arm64-cmake
            target-platform: windows
            windows-toolchain: mingw
            target-arch: aarch64
            msystem: CLANGARM64
            vcvars-arch: arm64
            cmake-preset: windows_mingw
            build-system: cmake

          # macOS builds
          - os: macos-15
            artifact-name: macOS-15-universal-cmake
            target-platform: macos
            target-arch: universal
            macosx-deployment-target: 12.0
            qt-architecture: clang_64
            qt-modules: qtimageformats qtnetworkauth qtpositioning qtserialport qtwebengine qtwebchannel
            qt-version: 6.10.2
            cmake-preset: windows_mingw
            build-system: cmake

          - os: macos-26
            artifact-name: macOS-26-universal-cmake
            target-platform: macos
            target-arch: universal
            macosx-deployment-target: 12.0
            qt-architecture: clang_64
            qt-modules: qtimageformats qtnetworkauth qtpositioning qtserialport qtwebengine qtwebchannel
            qt-version: 6.10.2
            cmake-preset: windows_mingw
            build-system: cmake

          - os: macos-15
            artifact-name: macOS-15-arm64-cmake
            target-platform: macos
            target-arch: aarch64
            macosx-deployment-target: 12.0
            qt-architecture: clang_64
            qt-modules: qtimageformats qtnetworkauth qtpositioning qtserialport qtwebengine qtwebchannel
            qt-version: 6.10.2
            cmake-preset: windows_mingw
            build-system: cmake

          - os: macos-26
            artifact-name: macOS-arm64-cmake
            target-platform: macos
            target-arch: aarch64
            macosx-deployment-target: 12.0
            qt-architecture: clang_64
            qt-modules: qtimageformats qtnetworkauth qtpositioning qtserialport qtwebengine qtwebchannel
            qt-version: 6.10.2
            cmake-preset: windows_mingw
            build-system: cmake

          - os: macos-15-intel
            artifact-name: macOS-intel-cmake
            target-platform: macos
            target-arch: x86_64
            macosx-deployment-target: 12.0
            qt-architecture: clang_64
            qt-modules: qtimageformats qtnetworkauth qtpositioning qtserialport qtwebengine qtwebchannel
            qt-version: 6.10.2
            cmake-preset: windows_mingw
            build-system: cmake

    runs-on: ${{ matrix.os }}

    env:
      ARTIFACT_NAME: ${{ matrix.artifact-name }}
      BUILD_PLATFORM: official
      CMAKE_PRESET: ${{ matrix.cmake-preset }}
      TARGET_PLATFORM: ${{ matrix.target-platform }}
      TARGET_ARCH: ${{ matrix.target-arch || 'x86_64' }}
      WINDOWS_TOOLCHAIN: ${{ matrix.windows-toolchain || 'mingw' }}
      MACOSX_DEPLOYMENT_TARGET: ${{ matrix.macosx-deployment-target }}
      MSYS_NO_PATHCONV: ${{ matrix.windows-toolchain == 'msvc' && '1' || '' }}
      MSYS2_ARG_CONV_EXCL: ${{ matrix.windows-toolchain == 'msvc' && '*' || '' }}

    defaults:
      run:
        shell: ${{ matrix.msystem && 'msys2 {0}' || 'bash' }}

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v6
        with:
          ref: ${{ env.CHECKOUT_REF }}
          submodules: true
          fetch-depth: 1

      - name: Setup Dependencies
        id: setup-dependencies
        uses: ./.github/actions/setup-dependencies
        with:
          artifact-name: ${{ matrix.artifact-name }}
          msystem: ${{ matrix.msystem }}
          vcvars-arch: ${{ matrix.vcvars-arch }}
          qt-architecture: ${{ matrix.qt-architecture }}
          qt-version: ${{ matrix.qt-version || inputs.qt-version || '6.10.0' }}
          qt-modules: ${{ matrix.qt-modules || '' }}

      - name: Set build type
        run: echo "BUILD_TYPE=${{ inputs.build-type || 'Debug' }}" >> $GITHUB_ENV

      # Windows MSVC Setup
      - name: Setup MSVC Environment (Windows MSVC)
        if: runner.os == 'Windows' && matrix.windows-toolchain == 'msvc'
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: ${{ matrix.vcvars-arch }}

      # Configure with Make system
      - name: Configure (Linux)
        if: runner.os == 'Linux' && matrix.build-system == 'make'
        run: |
          QT_PREFIX="${{ steps.setup-dependencies.outputs.qt-path }}"
          if [ -z "$QT_PREFIX" ]; then
            echo "ERROR: setup-dependencies did not provide qt-path" >&2
            exit 1
          fi
          ./configure \
            --platform=linux \
            --arch=${{ matrix.target-arch || 'x86_64' }} \
            --build=${{ env.BUILD_TYPE }} \
            --qt-prefix="$QT_PREFIX" \
            --with-tests

      - name: Configure (Windows MSVC)
        if: runner.os == 'Windows' && matrix.windows-toolchain == 'msvc' && matrix.build-system == 'make'
        shell: bash
        run: |
          QT_PREFIX="${{ steps.setup-dependencies.outputs.qt-path }}"
          if [ -z "$QT_PREFIX" ]; then
            echo "ERROR: setup-dependencies did not provide qt-path" >&2
            exit 1
          fi
          ./configure \
            --platform=windows \
            --build=${{ env.BUILD_TYPE }} \
            --arch=${{ matrix.target-arch || 'x86_64' }} \
            --qt-prefix="$QT_PREFIX" \
            --with-tests

      - name: Configure (Windows MinGW)
        if: runner.os == 'Windows' && matrix.windows-toolchain == 'mingw' && matrix.build-system == 'make'
        run: |
          if [ -n "${MSYSTEM:-}" ]; then
            msys_lc="$(echo "${MSYSTEM}" | tr '[:upper:]' '[:lower:]')"
            msys_bin="/${msys_lc}/bin"
            export PATH="${msys_bin}:$PATH"
            if [ -x "${msys_bin}/gcc" ] && [ -x "${msys_bin}/g++" ]; then
              export CC="gcc"
              export CXX="g++"
              export AR="ar"
              export RANLIB="ranlib"
            elif [ -x "${msys_bin}/clang" ] && [ -x "${msys_bin}/clang++" ]; then
              export CC="clang"
              export CXX="clang++"
              if [ -x "${msys_bin}/llvm-ar" ] && [ -x "${msys_bin}/llvm-ranlib" ]; then
                export AR="llvm-ar"
                export RANLIB="llvm-ranlib"
              else
                export AR="ar"
                export RANLIB="ranlib"
              fi
            fi
            hash -r
          fi
          export NO_CCACHE=1
          ./configure \
            --platform=windows \
            --arch=${{ matrix.target-arch || 'x86_64' }} \
            --build=${{ env.BUILD_TYPE }} \
            --with-tests

      - name: Configure (macOS)
        if: runner.os == 'macOS' && matrix.build-system == 'make'
        run: |
          QT_PREFIX="${{ steps.setup-dependencies.outputs.qt-path }}"
          if [ -z "$QT_PREFIX" ]; then
            echo "ERROR: setup-dependencies did not provide qt-path" >&2
            exit 1
          fi
          ./configure \
            --platform=macos \
            --arch=${{ matrix.target-arch || 'universal' }} \
            --build=${{ env.BUILD_TYPE }} \
            --qt-prefix="$QT_PREFIX" \
            --with-tests

      # Build with Make
      - name: Build (Linux/macOS/MinGW)
        if: matrix.build-system == 'make' && (runner.os != 'Windows' || matrix.windows-toolchain == 'mingw')
        run: |
          if [ "${RUNNER_OS}" = "Windows" ] && [ "${WINDOWS_TOOLCHAIN}" = "mingw" ] && [ -n "${MSYSTEM:-}" ]; then
            msys_lc="$(echo "${MSYSTEM}" | tr '[:upper:]' '[:lower:]')"
            msys_bin="/${msys_lc}/bin"
            export PATH="${msys_bin}:$PATH"
            if [ -x "${msys_bin}/gcc" ] && [ -x "${msys_bin}/g++" ]; then
              export CC="gcc"
              export CXX="g++"
              export AR="ar"
              export RANLIB="ranlib"
            elif [ -x "${msys_bin}/clang" ] && [ -x "${msys_bin}/clang++" ]; then
              export CC="clang"
              export CXX="clang++"
              if [ -x "${msys_bin}/llvm-ar" ] && [ -x "${msys_bin}/llvm-ranlib" ]; then
                export AR="llvm-ar"
                export RANLIB="llvm-ranlib"
              else
                export AR="ar"
                export RANLIB="ranlib"
              fi
            fi
            export NO_CCACHE=1
            hash -r
          fi
          make -j$(nproc 2>/dev/null || sysctl -n hw.ncpu 2>/dev/null || echo 4)

      - name: Build (Windows MSVC)
        if: runner.os == 'Windows' && matrix.windows-toolchain == 'msvc' && matrix.build-system == 'make'
        shell: bash
        run: |
          make -j${NUMBER_OF_PROCESSORS:-4}

      # Run Tests
      - name: Run Tests (Linux/macOS/MinGW)
        if: matrix.build-system == 'make' && (runner.os != 'Windows' || matrix.windows-toolchain == 'mingw')
        run: |
          if [ "${RUNNER_OS}" = "Windows" ] && [ "${WINDOWS_TOOLCHAIN}" = "mingw" ] && [ -n "${MSYSTEM:-}" ]; then
            msys_lc="$(echo "${MSYSTEM}" | tr '[:upper:]' '[:lower:]')"
            msys_bin="/${msys_lc}/bin"
            export PATH="${msys_bin}:$PATH"
            if [ -x "${msys_bin}/gcc" ] && [ -x "${msys_bin}/g++" ]; then
              export CC="gcc"
              export CXX="g++"
              export AR="ar"
              export RANLIB="ranlib"
            elif [ -x "${msys_bin}/clang" ] && [ -x "${msys_bin}/clang++" ]; then
              export CC="clang"
              export CXX="clang++"
              if [ -x "${msys_bin}/llvm-ar" ] && [ -x "${msys_bin}/llvm-ranlib" ]; then
                export AR="llvm-ar"
                export RANLIB="llvm-ranlib"
              else
                export AR="ar"
                export RANLIB="ranlib"
              fi
            fi
            export NO_CCACHE=1
            hash -r
          fi
          make test TEST_VERBOSE=1

      - name: Run Tests (Windows MSVC)
        if: runner.os == 'Windows' && matrix.windows-toolchain == 'msvc' && matrix.build-system == 'make'
        shell: bash
        run: |
          make test TEST_VERBOSE=1

      - name: CMake Configure
        if: matrix.build-system == 'cmake'
        run: |
          cmake --preset "$CMAKE_PRESET"

      - name: CMake Build
        if: matrix.build-system == 'cmake'
        run: |
          cmake --build --preset "$CMAKE_PRESET" --config "$BUILD_TYPE"

      - name: CMake Test
        if: matrix.build-system == 'cmake'
        run: |
          ctest --preset "$CMAKE_PRESET" --build-config "$BUILD_TYPE" --extra-verbose --output-on-failure

      # Upload Artifacts
      - name: Upload build
        uses: actions/upload-artifact@v6
        with:
          name: build-${{ matrix.artifact-name }}
          path: build/

      - name: Upload test results
        uses: actions/upload-artifact@v6
        with:
          name: test-results-${{ matrix.artifact-name }}
          path: build/tests/

      - name: Get short version
        id: short-version
        shell: bash
        run: |
          echo "version=$(git rev-parse --short HEAD)" >> "$GITHUB_OUTPUT"

      # Package
      - name: Package (Linux)
        if: runner.os == 'Linux'
        uses: ./.github/actions/package/linux
        with:
          version: ${{ steps.short-version.outputs.version }}
          build-type: ${{ env.BUILD_TYPE }}
          artifact-name: ${{ matrix.artifact-name }}
          qt-version: ${{ steps.setup-dependencies.outputs.qt-version }}
          gpg-private-key: ${{ secrets.LINUX_GPG_KEY }}
          gpg-private-key-id: ${{ secrets.LINUX_GPG_ID }}

      - name: Package (Windows)
        if: runner.os == 'Windows'
        uses: ./.github/actions/package/windows
        with:
          version: ${{ steps.short-version.outputs.version }}
          build-type: ${{ env.BUILD_TYPE }}
          artifact-name: ${{ matrix.artifact-name }}
          windows-codesign-cert: ${{ secrets.WINDOWS_CODESIGN_CERT }}
          windows-codesign-password: ${{ secrets.WINDOWS_CODESIGN_PASSWORD }}
          msystem: ${{ matrix.msystem }}

      - name: Package (macOS)
        if: runner.os == 'macOS'
        uses: ./.github/actions/package/macos
        with:
          version: ${{ steps.short-version.outputs.version }}
          build-type: ${{ env.BUILD_TYPE }}
          artifact-name: ${{ matrix.artifact-name }}
          apple-codesign-cert: ${{ secrets.APPLE-CODESIGN-CERT }}
          apple-codesign-password: ${{ secrets.APPLE-CODESIGN_PASSWORD }}
          apple-codesign-id: ${{ secrets.APPLE-CODESIGN_ID }}
          apple-notarize-apple-id: ${{ secrets.APPLE_NOTARIZE_APPLE_ID }}
          apple-notarize-team-id: ${{ secrets.APPLE_NOTARIZE_TEAM_ID }}
          apple-notarize-password: ${{ secrets.APPLE-NOTARIZE_PASSWORD }}
          sparkle-ed25519-key: ${{ secrets.SPARKLE_ED25519_PRIVATE_KEY }}
